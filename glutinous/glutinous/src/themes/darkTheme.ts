import { createTheme } from '@mui/material/styles';

const darkTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#FF7F50',
    },
    secondary: {
      main: '#FBF8BE',
    },
  },
});

export default darkTheme;
